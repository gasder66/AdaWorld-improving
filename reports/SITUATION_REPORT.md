# 关于"多主体隐动作分解"方向的情况说明

**日期**：2025-06-05

---

## 一、原始问题陈述

我们最初提出的改进动机是：

> "当画面中有多个物体时，产生的运动会互相抵消，导致学习出的隐向量不准确。"

因此，我们提出将单隐向量扩展为多主体隐动作，每个主体独立编码自己的动作。

---

## 二、实验验证的结果

### 2.1 无监督训练结果

在合成数据集（4个彩色方块，5类动作）上，使用 GT mask 进行 Mask-Guided 分解，纯无监督训练（重建 + KL，无动作标签）：

| 指标 | 有交互模块 | 无交互模块 | 随机基线 |
|------|-----------|-----------|---------|
| **KMeans ARI** | 0.0336 | 0.0577 | ~0.0 |
| **Cross-actor cos_sim** | >0.9 | >0.9 | - |
| **Linear probe** | 47.64% | 47.69% | 20% |

**关键发现**：
- 聚类指标 ARI ≈ 0.03，**接近完全随机**，说明不同主体的隐动作高度相似
- 线性探针 47% 说明隐动作确实编码了动作信息，但**无法区分不同主体**
- 交互模块让 cos_sim 从 0.8 降到 0.35（有监督时），但无监督下仍 >0.9

### 2.2 监督 vs 无监督对比

| 训练方式 | 动作准确率 | ARI | 说明 |
|---------|-----------|-----|------|
| 有动作监督 | 88% | - | **假象**：模型用标签"作弊" |
| 无监督 | 47%（线性探针） | 0.03 | **真实水平**：隐动作无法区分主体 |

---

## 三、对原始问题陈述的质疑

### 3.1 "运动互相抵消"是否成立？

**我认为这个问题本身未经验证，且可能不成立。理由如下：**

**（1）Spatio-Temporal Attention 已经建模了不同 patch 之间的关系**

原始 LAM 使用 SpatioTemporalTransformer 编码 patch 特征。注意力机制天然地让不同空间位置的 patch 互相交互——它不是简单的全局池化，而是保留了空间结构。所谓"运动互相抵消"是从人类直觉出发的理解，但注意力机制并不会把不同位置的运动"平均掉"。

**（2）V-JEPA 2 等工作证明了单向量路线的有效性**

Meta 的 V-JEPA 2（2025）使用单隐向量 + 强编码器，在 100 万小时视频上预训练后，成功实现了机器人零样本部署。如果"多主体运动抵消"是一个真实存在的问题，V-JEPA 2 不可能在如此复杂的真实场景中成功。

**（3）隐向量的"纠缠"不等于"没有学习到"**

单隐向量将多维度信息压缩在一起，确实会导致不同主体的信息纠缠。但纠缠 ≠ 没有学习到。就像 PCA 将高维数据压缩到低维，虽然维度纠缠，但信息仍然存在。线性探针 47%（随机基线 20%）说明信息确实被编码了，只是没有显式分离。

**（4）如果问题成立，LAM 应该在很多情况下都效果不好**

但事实上，原始 LAM 在单主体场景（机器人操作、驾驶等）上效果良好。多主体场景的"失败"更多是因为评估方式（我们期望每个主体有独立标签），而非模型本身无法处理。

### 3.2 即使问题成立，分解方案也面临根本困难

**（1）如何证明分解有效？**

FLAM 论文（ICLR 2026 投稿）做了和我们几乎相同的事情——用 Slot Attention 分解场景，每 slot 独立推断隐动作。但被 ICLR 拒绝，审稿人给出的核心批评是：

> "论文声称 FLAM 基于动力学而非视觉外观分组实体，但缺乏定性证据支持。"

审稿分数：4-2-2-2。这说明**即使做了分解，也难以证明它确实按动力学分组**，而非按外观分组。

**（2）我们的实验直接证明了分解失败**

在合成数据上，无监督训练后 ARI=0.03，不同主体的隐动作 cos_sim > 0.9。这说明**即使给了 GT mask 做分解，模型也不会自发地让不同主体的隐动作分离**。根本原因是场景级重建损失不提供分离信号。

**（3）要让分离成功，必须大幅修改损失函数**

要驱动分离，需要引入对象级重建、对比损失、时序预测等。但这意味着：
- 架构复杂度大幅增加（每 slot 独立解码器）
- 损失函数设计变得复杂（多个损失项的权重平衡）
- 引入的归纳偏置可能过强（人为假设"每主体独立"）

---

## 四、多向量输出的实用性问题

### 4.1 下游世界模型如何使用多向量？

原始 LAM 产生的单隐向量直接作为世界模型的"动作输入"。但多隐向量不能这样用：

| 方案 | 问题 |
|------|------|
| 拼接成 1 个长向量 | 维度爆炸，且主体数量变化时维度不固定 |
| 求和/平均 | 丢失独立信息，退化为单向量 |
| 世界模型接收序列 | 需要重新设计世界模型，耦合度高 |

FLAM 的解决方案是**自身就是世界模型**（每 slot 独立预测下一状态），但这意味着我们必须同时改造世界模型，工作量翻倍。

### 4.2 主体数量难以确定

- 不同视频的主体数量不同
- 同一视频的不同帧，主体可能进出画面
- FLAM 使用固定 K（超参数），但 ICLR 审稿人批评"K 的影响分析不足"
- 动态 K 需要额外的机制（如注意力 mask），进一步增加复杂度

### 4.3 信息瓶颈被打破

JEPA 的核心思想是通过信息瓶颈（单隐向量）迫使模型学习最重要的信息。多向量输出打破了这一瓶颈：
- 单向量：32 维，模型必须压缩
- 4 主体 × 32 维 = 128 维，信息量 ×4
- 这让模型失去了"必须压缩"的压力，变得"四不像"

---

## 五、结论

### 我认为"多主体隐动作分解"这个 idea 不 work，理由总结如下：

1. **原始问题未验证**："多主体运动抵消"缺乏实证支持，V-JEPA 2 证明单向量可以处理复杂场景

2. **实验直接证伪**：无监督训练后 ARI=0.03，隐动作无法区分不同主体。即使给了 GT mask 做分解，场景级重建损失也不提供分离信号

3. **FLAM 的前车之鉴**：做同样方向的论文被 ICLR 拒绝（4-2-2-2），核心批评是"缺乏证据证明分解确实按动力学分组"

4. **实用性差**：多向量输出与现有世界模型接口不兼容，主体数量难以确定，信息瓶颈被打破

5. **复杂度不值得**：要让分解真正工作，需要对象级重建 + 对比损失 + 时序预测，架构和损失函数都大幅复杂化，但收益不确定

### 建议

与其在"多主体分解"上继续投入，不如考虑以下替代方向：

1. **增强单向量 LAM 的编码能力**：更强的编码器、更长的时序建模、更好的数据增强
2. **利用多主体信息作为辅助信号**：用检测/分割结果作为额外输入条件，但不改变单向量输出
3. **层次化设计**：单全局向量 + 可选的主体级细节向量，保持信息瓶颈

---

**说明人**：[姓名]
**日期**：2025-06-05
